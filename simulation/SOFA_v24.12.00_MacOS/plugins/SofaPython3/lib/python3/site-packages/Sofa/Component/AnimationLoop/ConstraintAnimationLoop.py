# -*- coding: utf-8 -*-


"""
Component ConstraintAnimationLoop

.. autofunction:: ConstraintAnimationLoop

Indices and tables
******************

* :ref:`genindex`
* :ref:`modindex`
* :ref:`search`
"""
import Sofa
from Sofa.Core import Object, LinkPath
from Sofa.Component.TypeHints import Data, Optional, SofaArray
import dataclasses
    
class ConstraintAnimationLoop(Object):
    """Constraint animation loop manager"""

    def __init__(self, template: Optional[str] = None, name: str,printLog: bool,tags: object,bbox: object,componentState: object,listening: bool,computeBoundingBox: bool,displayTime: bool,tolerance: float,maxIterations: int,doCollisionsFirst: bool,doubleBuffer: bool,scaleTolerance: bool,allVerified: bool,sor: float,schemeCorrection: bool,realTimeCompensation: bool,graphErrors: TypeHints.SofaArray,graphConstraints: TypeHints.SofaArray,graphForces: TypeHints.SofaArray):
        """Constraint animation loop manager
        
        Args:
           allVerified (bool):   All constraints must be verified (each constraint's error < tolerance)
           bbox (object):   this object bounding box
           componentState (object):   The state of the component among (Dirty, Valid, Undefined, Loading, Invalid).
           computeBoundingBox (bool):   If true, compute the global bounding box of the scene at each time step. Used mostly for rendering.
           displayTime (bool):   Display time for each important step of ConstraintAnimationLoop.
           doCollisionsFirst (bool):   Compute the collisions first (to support penality-based contacts)
           doubleBuffer (bool):   Double the buffer dedicated to the constraint problem to make it accessible to another thread
           graphConstraints (TypeHints.SofaArray):   Graph of each constraint's error at the end of the resolution
           graphErrors (TypeHints.SofaArray):   Sum of the constraints' errors at each iteration
           graphForces (TypeHints.SofaArray):   Graph of each constraint's force at each step of the resolution
           listening (bool):   if true, handle the events, otherwise ignore the events
           maxIterations (int):   Maximum number of iterations of the Gauss-Seidel
           name (str):   object name
           printLog (bool):   if true, emits extra messages at runtime.
           realTimeCompensation (bool):   If the total computational time T < dt, sleep(dt-T)
           scaleTolerance (bool):   Scale the error tolerance with the number of constraints
           schemeCorrection (bool):   Apply new scheme where compliance is progressively corrected
           sor (float):   Successive Over Relaxation parameter (0-2)
           tags (object):   list of the subsets the object belongs to
           tolerance (float):   Tolerance of the Gauss-Seidel
           template (str): the type of degree of freedom
        """
        ...

    name: Data[str] 
    'object name'

    printLog: Data[bool] 
    'if true, emits extra messages at runtime.'

    tags: Data[object] 
    'list of the subsets the object belongs to'

    bbox: Data[object] 
    'this object bounding box'

    componentState: Data[object] 
    'The state of the component among (Dirty, Valid, Undefined, Loading, Invalid).'

    listening: Data[bool] 
    'if true, handle the events, otherwise ignore the events'

    computeBoundingBox: Data[bool] 
    'If true, compute the global bounding box of the scene at each time step. Used mostly for rendering.'

    displayTime: Data[bool] 
    'Display time for each important step of ConstraintAnimationLoop.'

    tolerance: Data[float] 
    'Tolerance of the Gauss-Seidel'

    maxIterations: Data[int] 
    'Maximum number of iterations of the Gauss-Seidel'

    doCollisionsFirst: Data[bool] 
    'Compute the collisions first (to support penality-based contacts)'

    doubleBuffer: Data[bool] 
    'Double the buffer dedicated to the constraint problem to make it accessible to another thread'

    scaleTolerance: Data[bool] 
    'Scale the error tolerance with the number of constraints'

    allVerified: Data[bool] 
    'All constraints must be verified (each constraints error < tolerance)'

    sor: Data[float] 
    'Successive Over Relaxation parameter (0-2)'

    schemeCorrection: Data[bool] 
    'Apply new scheme where compliance is progressively corrected'

    realTimeCompensation: Data[bool] 
    'If the total computational time T < dt, sleep(dt-T)'

    graphErrors: Data[TypeHints.SofaArray] 
    'Sum of the constraints errors at each iteration'

    graphConstraints: Data[TypeHints.SofaArray] 
    'Graph of each constraints error at the end of the resolution'

    graphForces: Data[TypeHints.SofaArray] 
    'Graph of each constraints force at each step of the resolution'



